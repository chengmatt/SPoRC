# The composition likelihoods a fleet can be given

The composition likelihoods a fleet can be given

## Usage

``` r
comp_like_type_options()
```

## Value

Character vector of accepted values, in the order the message lists
them.
