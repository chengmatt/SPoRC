# Bin midpoints from lower edges, the last bin taking the width of the one before

Bin midpoints from lower edges, the last bin taking the width of the one
before

## Usage

``` r
growth_len_mid(len_lower)
```
