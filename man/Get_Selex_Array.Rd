% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_selectivity.R
\name{Get_Selex_Array}
\alias{Get_Selex_Array}
\title{Build the full selectivity array for one selectivity type (retention, fishery, survey)}
\usage{
Get_Selex_Array(
  selex_type,
  bins,
  sel_blocks,
  sel_model,
  fixed_sel_pars,
  cont_tv_sel,
  ln_seldevs,
  use_fixed_sel,
  sel_input,
  bicubic_Wbin,
  bicubic_Wyr,
  bicubic_binnodes,
  bicubic_yrnodes,
  n_pop,
  n_regions,
  n_yrs,
  n_proj_yrs_devs,
  n_seas,
  n_ages,
  n_lens,
  n_sexes,
  n_fleets,
  bin_devs = NULL,
  bin_dev_bins = NULL,
  sel_norm_bins = NULL
)
}
\arguments{
\item{selex_type}{Integer switch (0 = age-based, 1 = length-based).}

\item{bins}{Numeric vector of bins; \code{ages} when \code{selex_type == 0}
and \code{lens} when \code{selex_type == 1}.}

\item{sel_blocks}{Integer array \code{n_regions x n_yrs x n_fleets} of
selectivity block indices.}

\item{sel_model}{Integer array \code{n_regions x n_yrs x n_fleets} of
selectivity functional forms (see \code{\link{Get_Selex}}).}

\item{fixed_sel_pars}{Array \code{n_regions x n_pars x n_blocks x n_sexes x
n_fleets} of fixed-effect selectivity parameters.}

\item{cont_tv_sel}{Integer matrix \code{n_regions x n_fleets} of continuous
time-varying forms.}

\item{ln_seldevs}{Array \code{n_regions x n_yrs_total x n_bins x n_sexes x
n_fleets} of selectivity deviations; sliced per fleet internally.}

\item{use_fixed_sel}{Integer vector of length \code{n_fleets} (0 = estimate,
1 = use \code{sel_input}).}

\item{sel_input}{Array \code{n_pop x n_regions x n_yrs x n_seas x n_bins x
n_sexes x n_fleets} of fixed selectivity values.}

\item{bicubic_Wbin, bicubic_Wyr}{Bicubic spline bin-node and year-node weight
arrays (\code{Selex_Model == 8} only).}

\item{bicubic_binnodes, bicubic_yrnodes}{Integer arrays \code{n_regions x
n_yrs x n_fleets} giving each block's true node counts
(\code{Selex_Model == 8} only).}

\item{n_pop, n_regions, n_yrs, n_proj_yrs_devs, n_seas, n_ages, n_lens, n_sexes, n_fleets}{Model dimensions.}
}
\value{
List with \code{sel}, the age-based array (\code{n_pop x n_regions x
  n_yrs_total x n_seas x n_ages x n_sexes x n_fleets}), and \code{sel_l}, the
  length-based array (\code{n_regions x n_yrs_total x n_lens x n_sexes x
  n_fleets}). Only the array matching \code{selex_type} is populated; when
  \code{selex_type == 1}, \code{sel} is filled downstream by multiplying
  \code{sel_l} through the size-age transition matrix.
}
\description{
Evaluates \code{\link{Get_Selex}} across regions, years, fleets, and sexes
for a single selectivity type, and mean standardizes the result where the
time-varying or non-parametric form calls for it. Total fishery, retention,
and survey selectivity all share this code path and differ only in which
data arrays are handed in.
}
\keyword{internal}
