% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_priors_penalties.R
\name{Get_q_dev_penalty}
\alias{Get_q_dev_penalty}
\title{Penalty on catchability deviations}
\usage{
Get_q_dev_penalty(
  ln_q_devs,
  ln_sigma_q,
  q_rho,
  q_model,
  map_ln_q_devs = NULL,
  q_rw_init_sigma = NA
)
}
\arguments{
\item{ln_q_devs}{Array \code{[n_regions, n_yrs_total, n_fleets]} of log-scale
catchability deviations.}

\item{ln_sigma_q}{Array \code{[n_regions, n_fleets]} of log-scale deviation
standard deviations.}

\item{q_rho}{Array \code{[n_regions, n_fleets]} of unconstrained ar1
correlations, read only where \code{q_model} is ar1.}

\item{q_model}{Integer vector \code{[n_fleets]}: 1 none, 2 iid, 3 random
walk, 4 ar1, 5 dsem.}

\item{map_ln_q_devs}{Numeric array of map levels the same shape as
\code{ln_q_devs}, \code{NA} where a cell is not penalized.}

\item{q_rw_init_sigma}{Standard deviation of the first estimated year of a
random walk, or \code{NA} to start it at zero under its own sigma.}
}
\value{
Negative log density, summed over regions, years and fleets.
}
\description{
Annual deviations from a fleet's block catchability, taken as independent,
a random walk or an ar1. A fleet whose deviations a dsem has
taken over reads no penalty here, since the mirror blanks those cells.
}
\keyword{internal}
