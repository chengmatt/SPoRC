% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_naa_state.R
\name{Setup_Sim_NAA_state}
\alias{Setup_Sim_NAA_state}
\title{Specify the state-space numbers-at-age process for simulation}
\usage{
Setup_Sim_NAA_state(
  sim_list,
  NAA_re = "none",
  sigmaNAA = 0.3,
  rho_age = 0,
  rho_year = 0,
  rho_cohort = 0,
  NAA_re_pop = "iid",
  NAA_re_region = "iid",
  NAA_re_sex = "iid",
  pop_corr = 0,
  region_corr = 0,
  sex_corr = 0,
  NAA_re_ages = NULL,
  NAA_re_years = NULL
)
}
\arguments{
\item{sim_list}{Simulation list from \code{\link{Setup_Sim_Dim}}.}

\item{NAA_re}{Character. \code{"none"} (default) leaves the numbers at age
deterministic past recruitment and the initial age structure. Otherwise one
of \code{"iid"}, \code{"1dar1_a"}, \code{"1dar1_y"}, \code{"2dar1"},
\code{"3dcond"} or \code{"3dmarg"}.}

\item{sigmaNAA}{Numeric. Conditional standard deviation of the innovations,
the same quantity \code{ln_sigmaNAA} holds in the estimation model. Under an
autoregressive form the marginal standard deviation is larger by
\eqn{1/\sqrt{1 - \rho^2}} per correlated margin.}

\item{rho_age, rho_year, rho_cohort}{Numeric correlations in \eqn{(-1, 1)} over
the age, year and cohort margins. Only the ones the chosen form reads are
used.}

\item{NAA_re_pop, NAA_re_region, NAA_re_sex}{Character, \code{"iid"} (default) or
\code{"us"}, an unstructured correlation across that margin.}

\item{pop_corr, region_corr, sex_corr}{Numeric vectors of length \eqn{n(n-1)/2}
giving the correlations for those margins, ordered as the strict lower
triangle is filled by column. A single value is recycled.}

\item{NAA_re_ages, NAA_re_years}{Ages and year indices the state covers.
\code{NULL} (default) uses everything from the second onward.}
}
\value{
\code{sim_list} with the state-space settings attached.
}
\description{
Turns on process error in the numbers at age for a simulated population. The
operating model applies the same centered state the estimation model does:
the deterministic mortality and ageing step is computed, then the numbers are
multiplied by \eqn{\exp(\eta)} with \eqn{\eta} drawn from the covariance the
arguments here describe.
}
\details{
Arguments mirror \code{Setup_Mod_Biologicals}'s state-space options so a
simulated population and the model fitted to it are written the same way, which
is what makes a self test a like-for-like comparison rather than a translation.
}
\seealso{
Other Simulation Setup:
\code{\link[=Setup_Sim_Biologicals]{Setup_Sim_Biologicals()}},
\code{\link[=Setup_Sim_Containers]{Setup_Sim_Containers()}},
\code{\link[=Setup_Sim_Dim]{Setup_Sim_Dim()}},
\code{\link[=Setup_Sim_Fishing]{Setup_Sim_Fishing()}},
\code{\link[=Setup_Sim_Rec]{Setup_Sim_Rec()}},
\code{\link[=Setup_Sim_Survey]{Setup_Sim_Survey()}},
\code{\link[=Setup_Sim_Tagging]{Setup_Sim_Tagging()}},
\code{\link[=Setup_sim_env]{Setup_sim_env()}},
\code{\link[=Simulate_Pop_Static]{Simulate_Pop_Static()}},
\code{\link[=run_annual_cycle]{run_annual_cycle()}},
\code{\link[=simulation_self_test]{simulation_self_test()}}
}
\concept{Simulation Setup}
