% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_priors_penalties.R
\name{dev_share_weights}
\alias{dev_share_weights}
\title{Share of one penalty owed by each cell of a mapped deviation array}
\usage{
dev_share_weights(map, dims)
}
\arguments{
\item{map}{Numeric array of map levels, \code{NA} where the cell is fixed, or
\code{NULL} when every cell holds its own parameter.}

\item{dims}{Dimensions the weights are returned on.}
}
\value{
Numeric array of weights over \code{dims}, all ones when \code{map} is
  \code{NULL}.
}
\description{
Cells sharing a map level hold one parameter between them, so penalizing
every cell would count that parameter once per cell. Each cell takes one over
the number of cells at its level, and a cell mapped off takes zero.
}
\keyword{internal}
