% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_lik_caal.R
\name{eval_caal_osa}
\alias{eval_caal_osa}
\title{Evaluate CAAL likelihoods from a tracked OBS vector (OSA)}
\usage{
eval_caal_osa(
  nLL_arr,
  tracked,
  ExpArrFn,
  UseArr,
  TypeMat,
  LikeTypeVec,
  ISSArr,
  lnThetaArr,
  lnThetaAggVec,
  n_regions,
  n_yrs,
  n_seas,
  n_lens,
  n_fleets,
  n_sexes,
  n_model_bins,
  n_obs_bins,
  AgeingErrorFn,
  addtocomp,
  BinsArr = NULL
)
}
\arguments{
\item{nLL_arr}{Array \eqn{[region \times year \times season \times len \times
sex \times fleet]} receiving negative log-likelihood contributions.}

\item{tracked}{Flat tracked OBS vector from \code{\link{pack_caal_osa}}.}

\item{ExpArrFn}{Function of \code{(y, seas, l, f)} returning the expected joint
numbers at length and age for that group, indexed
\eqn{[region \times age \times sex]}.}

\item{UseArr}{Use flags \eqn{[region \times year \times season \times len
\times fleet]}.}

\item{TypeMat}{Composition type matrix \eqn{[year \times fleet]}.}

\item{LikeTypeVec}{Likelihood type per fleet.}

\item{ISSArr}{Input sample sizes \eqn{[region \times year \times season \times
len \times sex \times fleet]}.}

\item{lnThetaArr}{Log overdispersion \eqn{[region \times sex \times fleet]}.}

\item{lnThetaAggVec}{Aggregated log overdispersion, one per fleet.}

\item{n_regions, n_yrs, n_seas, n_lens, n_fleets, n_sexes}{Dimension sizes.}

\item{n_model_bins}{Number of model age bins.}

\item{n_obs_bins}{Number of observed age bins.}

\item{AgeingErrorFn}{Function \code{(y, f)} returning the ageing error matrix
for a given year and fleet.}

\item{addtocomp}{Small constant added to proportions before normalization.}

\item{BinsArr}{Optional \code{[n_obs_bins x n_fleets]} 0/1 array naming the
observed age bins each fleet is fitted over, or \code{NULL} (default) for
all bins. Must be the same array handed to \code{\link{pack_caal_osa}},
since the strides walked here are sized on it.}
}
\value{
Updated \code{nLL_arr}.
}
\description{
Walks the flat vector built by \code{\link{pack_caal_osa}} in the same order
and evaluates each length bin's age composition through
\code{\link{Get_Comp_Likelihoods_OSA}}, so the observations stay on the tape as
the tracked quantity \code{RTMB::oneStepPredict} needs.
}
\keyword{internal}
