% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_precision.R
\name{fill_dsem_derived}
\alias{fill_dsem_derived}
\title{Work out the time-series where the sd is fixed at zero}
\usage{
fill_dsem_derived(x_grid, mu_grid, arrow_value, dsem_model, only = NULL)
}
\arguments{
\item{x_grid}{Matrix \code{[year, series]}, the derived columns overwritten.}

\item{mu_grid}{Matrix \code{[year, series]} of series means.}

\item{arrow_value}{Output of \code{\link{get_dsem_arrow_values}}.}

\item{dsem_model}{Output of \code{\link{read_dsem_arrows}}.}

\item{only}{Integer vector of series to fill, or \code{NULL} (default) for
every derived series. Used to fill the catchability series before the
observations are built, since the rest need the population first.}
}
\value{
\code{x_grid} with every derived cell set.
}
\description{
A series with an sd of zero has no innovation, so its value each year is
exactly what the arrows pointing into it give: its own mean plus, for every
path into it, that arrow's coefficient times how far the series it reads
sits from its mean in the year it reads. Series are visited in an order that
sets each one before anything reads it (\code{series_order}), one year at a
time.
}
\keyword{internal}
