% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_growth.R
\name{get_alk}
\alias{get_alk}
\title{Binned age-length key}
\usage{
get_alk(len_lower, mu, sd, dist = 0)
}
\arguments{
\item{len_lower}{Numeric vector of lower bin edges.}

\item{mu}{Mean length at age, possibly AD.}

\item{sd}{Spread of length at age, possibly AD.}

\item{dist}{Integer, 0 normal, 1 lognormal (\code{sd} on the log scale, mean
corrected so the arithmetic mean stays \code{mu}).}
}
\value{
Matrix \code{[n_lens x n_ages]}.
}
\description{
\eqn{P(l \mid a)} on the length bins given by their lower edges: the normal (or lognormal) CDF is taken at every lower edge,
differenced, and the tails below the first edge and above the last are
accumulated into the end bins, so every column sums to one.
}
\keyword{internal}
