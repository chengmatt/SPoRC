% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_lik_at_age.R
\name{get_at_age_obs_prediction}
\alias{get_at_age_obs_prediction}
\title{Predicted values at the ages the observations are recorded on}
\usage{
get_at_age_obs_prediction(
  source,
  arrays,
  p_idx,
  r_idx,
  s_idx,
  y,
  seas,
  obs_ages,
  f,
  ageing_error = NULL
)
}
\arguments{
\item{source, arrays, p_idx, r_idx, s_idx, y, seas, f}{See
\code{\link{get_at_age_prediction}}.}

\item{obs_ages}{Integer vector of the observed ages to predict.}

\item{ageing_error}{Array \code{[n_years, n_ages, n_obs_ages]} for this
fleet, or \code{NULL} when the observed ages are the model ages.}
}
\value{
A vector the length of \code{obs_ages}.
}
\description{
An at-age observation counts fish by the age they were read as, so the prediction at each
observed age sums the predictions at every model age read as it, weighted by the fleet's
ageing error matrix, with units and discard mortality applied at the model age first.
}
\keyword{internal}
