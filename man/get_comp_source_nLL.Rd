% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_lik_comps.R
\name{get_comp_source_nLL}
\alias{get_comp_source_nLL}
\title{Evaluate one composition data source through the direct likelihood}
\usage{
get_comp_source_nLL(
  nLL_arr,
  ObsArr,
  ExpArr,
  UseArr,
  ISSArr,
  WtArr,
  TypeMat,
  LikeTypeVec,
  lnThetaArr,
  lnThetaAggVec,
  LNcorrArr,
  LNcorrAggVec,
  age_or_len,
  n_model_bins,
  comp_bins_spec,
  AgeingErrorArr = NULL,
  LenBinMap_lik = NA,
  LenBinMap_fn = NULL,
  n_pop,
  n_regions,
  n_yrs,
  n_seas,
  n_fleets,
  n_sexes,
  pop = FALSE,
  addtocomp = 0,
  comp_const_obs = 1,
  do_internal_comp_osa = FALSE,
  tracked_discrete = NULL,
  tracked_continuous = NULL
)
}
\arguments{
\item{nLL_arr}{Container for this data source's negative log likelihood, dimensioned
region by year by season by sex by fleet, with a leading population dimension
when \code{pop} is \code{TRUE}.}

\item{ObsArr}{Observed compositions.}

\item{ExpArr}{Predicted numbers the compositions are formed from: catch at age
or length, discards at age or length, or survey available numbers.}

\item{UseArr}{Integer array flagging which cells are fit.}

\item{ISSArr}{Input sample size.}

\item{WtArr}{Likelihood weight applied to a multinomial.}

\item{TypeMat}{Integer codes naming how each year and fleet's compositions are
split, over year by fleet.}

\item{LikeTypeVec}{Integer likelihood per fleet.}

\item{lnThetaArr, lnThetaAggVec}{Dirichlet-multinomial dispersion, for the split
and the aggregated compositions.}

\item{LNcorrArr, LNcorrAggVec}{Logistic normal correlation parameters, for the
split and the aggregated compositions.}

\item{age_or_len}{\code{0} for age compositions, \code{1} for length.}

\item{n_model_bins}{Number of model bins, \code{n_ages} or \code{n_lens}.}

\item{comp_bins_spec}{Array of 0/1 flags over bin by fleet naming the bins this
data source is fit over. All ones means the whole range.}

\item{AgeingErrorArr}{Ageing error over year by model bin by observed bin by
fleet, read only for age compositions.}

\item{LenBinMap_lik}{Model bin to observed bin map, read only for length
compositions.}

\item{LenBinMap_fn}{Function of year and fleet returning the model bin to
observed bin map, read only for length compositions on the OSA route.}

\item{n_pop, n_regions, n_yrs, n_seas, n_fleets, n_sexes}{Model dimensions.}

\item{pop}{Logical. \code{TRUE} for the population-specific data source, whose arrays
have a leading population dimension and are never summed over populations.}

\item{addtocomp}{Small constant added to a composition.}

\item{comp_const_obs}{Constant the observations are scaled by.}

\item{do_internal_comp_osa}{Logical. \code{TRUE} hands the data source to
\code{\link{eval_comp_source_osa}}, which reads the vectors
\code{\link{pack_comp_source_osa}} built and the call site registered.}

\item{tracked_discrete, tracked_continuous}{Registered observation vectors, read
only on the OSA route and \code{NULL} where no fleet uses that family.}
}
\value{
\code{nLL_arr}, filled in.
}
\description{
Walks every year, season, fleet and, for a population-specific data source, every
population in one composition data source, and calls
\code{\link{Get_Comp_Likelihoods}} on each cell that is fit. One call stands
for what used to be written out separately for retained fishery, discarded
fishery and survey compositions, for ages and for lengths, and again for the
regional and the population-specific data source of each.
}
\keyword{internal}
