% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup_dsem.R
\name{get_dsem_Q_oo_pattern}
\alias{get_dsem_Q_oo_pattern}
\title{Figure out which entries of the dsem precision can be non-zero}
\usage{
get_dsem_Q_oo_pattern(IminusB_m, Gamma_m, obs_idx, unobs_idx, has_cov)
}
\arguments{
\item{IminusB_m}{Numbered sparse template of \eqn{I - B}, from
\code{\link{get_dsem_cells}}, read for where the paths sit.}

\item{Gamma_m}{Numbered sparse template of \eqn{\Gamma}. Not read: the
covariance entries only matter here through \code{has_cov}.}

\item{obs_idx, unobs_idx}{Cells that keep a deviation of their own, and cells
of a series with an sd of zero, which are solved out of them.}

\item{has_cov}{Whether any arrow is a covariance.}
}
\value{
\code{NULL} when nothing is solved out, since the precision then has
  \eqn{I - B}'s own entries. Otherwise a list with \code{m}, the numbered
  sparse template, and \code{slot_lin}, where each stored entry sits in the
  dense matrix counted down the columns.
}
\description{
\code{RTMB::dgmrf} takes the precision (the inverse of the covariance) as a
sparse matrix, and on the tape only the entries it already stores can be
filled, so the positions are fixed here at setup from the arrows alone.
}
\keyword{internal}
