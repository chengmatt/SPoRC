% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup_dsem.R
\name{get_dsem_link}
\alias{get_dsem_link}
\title{Series a dsem can link, and where each one's cells sit}
\usage{
get_dsem_link(input_list, dsem_processes, arrow_text, n_grid_yrs)
}
\arguments{
\item{input_list}{List with \code{data}, \code{par} and \code{map}.}

\item{dsem_processes}{Labels from \code{dsem_process_table} to offer.}

\item{arrow_text}{The arrow lines as one string, comments already removed.}

\item{n_grid_yrs}{Rows of the dsem grid.}
}
\value{
List with \code{offered} (every name the processes could give, with
  \code{offered_label}, \code{offered_par} and \code{offered_cell}) and,
  for the linked ones, \code{name}, \code{par}, \code{label}, \code{sigma_par},
  \code{penalty_reads_map}, \code{grid_row} (the grid rows that array
  reaches) and \code{cell} (where each of those rows sits in the flattened
  array).
}
\description{
One series per deviation array, kept at every index except the year:
\code{ln_growth_devs} of a two sex model gives one series per population,
region, parameter and sex. An arrow names the ones to link.
}
\keyword{internal}
