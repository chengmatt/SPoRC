% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_precision.R
\name{get_dsem_precision}
\alias{get_dsem_precision}
\title{The precision of the whole grid}
\usage{
get_dsem_precision(
  dsem_beta,
  ln_dsem_sd,
  dsem_model,
  dsem_cells,
  x_grid = NULL
)
}
\arguments{
\item{dsem_beta, ln_dsem_sd, dsem_model}{As in \code{\link{get_dsem_arrow_values}}.}

\item{dsem_cells}{Output of \code{\link{get_dsem_cells}}.}

\item{x_grid}{Matrix \code{[year, series]} of the grid, needed when an arrow
is moderated.}
}
\value{
Sparse precision matrix over the \code{n_grid_yrs * n_series} cells.
}
\description{
\eqn{Q = (I - B)^{\top} V^{-1} (I - B)}, the inverse covariance of the
stacked grid: if the innovations have covariance \eqn{V} and the grid is
\eqn{x - \mu = (I - B)^{-1}\varepsilon}, then the grid's covariance is
\eqn{(I - B)^{-1} V (I - B)^{-\top}} and its inverse is this product. It is
the same form \code{\link{Get_3d_precision}} builds for the numbers at age
field. \code{RTMB::dgmrf} evaluates the density from it without forming the
covariance.
}
\keyword{internal}
