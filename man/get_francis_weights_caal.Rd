% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diag_francis.R
\name{get_francis_weights_caal}
\alias{get_francis_weights_caal}
\title{Francis weights for conditional age-at-length compositions}
\usage{
get_francis_weights_caal(
  n_regions,
  n_sexes,
  n_fleets,
  n_years,
  n_seas,
  n_lens,
  Use,
  ISS,
  Pred_array,
  Obs_array,
  weights,
  bins,
  comp_type
)
}
\arguments{
\item{n_regions}{Number of regions}

\item{n_sexes}{Number of sexes}

\item{n_fleets}{Number of fleets (fishery or survey)}

\item{n_years}{Number of years}

\item{n_seas}{Number of seasons}

\item{n_lens}{Number of length bins}

\item{Use}{Use flags \code{[region, year, season, len, fleet]}}

\item{ISS}{Input sample sizes \code{[region, year, season, len, sex, fleet]}}

\item{Pred_array}{Predicted proportions \code{[region, year, season, len, bin, sex, fleet]}}

\item{Obs_array}{Observed proportions, same shape as \code{Pred_array}}

\item{weights}{Array of weights to fill, shaped like \code{ISS}}

\item{bins}{Vector of age bins the compositions are over}

\item{comp_type}{Matrix of composition types \code{[year, fleet]}}
}
\value{
List with the filled \code{weights} and a data frame of the observed
  and expected mean ages behind them
}
\description{
A conditional age-at-length row is the age composition of the fish aged from
one length bin, so a fleet's samples are the (year, season, length bin) rows
rather than the years alone. The Francis statistic is the same standardized
mean-age residual used for the marginal compositions, pooled over every row a
fleet has, which gives one weight per fleet (per region and sex where the
composition type splits them). A weight per length bin would rest on far fewer
samples and would not be stable.
}
\keyword{internal}
