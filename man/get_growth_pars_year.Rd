% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_growth.R
\name{get_growth_pars_year}
\alias{get_growth_pars_year}
\title{Growth parameters in effect in one year}
\usage{
get_growth_pars_year(ln_pars, ln_devs, tv_model, tv_link, bounds, y)
}
\arguments{
\item{ln_pars}{Log-scale base parameters of the stratum, length
\code{n_gpars} (five for the von Bertalanffy form, six with the Richards
coefficient last).}

\item{ln_devs}{Matrix \code{[n_yrs x n_gpars]} of the stratum's deviations.}

\item{tv_model}{Integer vector \code{[n_gpars]}, 0 constant, 1 iid
deviations, 2 random walk.}

\item{tv_link}{Integer, 0 log link, 1 logit link within \code{bounds}.}

\item{bounds}{Matrix \code{[n_gpars x 2]} of lower and upper bounds, read
under the logit link.}

\item{y}{Year index.}
}
\value{
Natural-scale parameter vector for the year, with a seventh element
  \code{rho} of one when the base has five parameters.
}
\description{
Applies the time-varying deviations of one stratum to its base parameters.
Under the log link a parameter in year \eqn{y} is \eqn{P \exp(\delta_y)};
under the logit link it is kept inside its bounds,
\deqn{P_y = lo + (hi - lo)\,\mathrm{logit}^{-1}\!\left(\log\frac{P - lo}{hi - P} + \delta_y\right)}
A random walk's deviation array holds the walk's position, so both
structures read the same way here and differ only in their penalty.
}
\keyword{internal}
