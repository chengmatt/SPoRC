% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup_recruitment.R
\name{get_rec_bias_ramp}
\alias{get_rec_bias_ramp}
\title{The recruitment bias ramp, year by year}
\usage{
get_rec_bias_ramp(
  do_rec_bias_ramp,
  bias_year,
  n_est_rec_devs,
  max_bias_ramp_fct = 1
)
}
\arguments{
\item{do_rec_bias_ramp}{Integer, \code{0} or \code{1}.}

\item{bias_year}{Integer vector of the four ramp years, as deviation indices.}

\item{n_est_rec_devs}{Number of estimated recruitment deviation years.}

\item{max_bias_ramp_fct}{Scale of the ramp at its plateau.}
}
\value{
Numeric vector of length \code{n_est_rec_devs}.
}
\description{
The lognormal bias correction a recruitment deviation's penalty is centered
on, \eqn{-b_t \sigma^2 / 2}, as a factor \eqn{b_t} per estimated year.
\code{do_rec_bias_ramp = 0} gives the full correction in every year
(\eqn{b_t = 1}); \code{1} ramps it up, holds it and ramps it down over the
four \code{bias_year} indices, scaled by \code{max_bias_ramp_fct}, so a
ramp whose years all sit at the last year is zero everywhere. Used by the
objective and by the setup checks that ask whether the correction touches a
given year.
}
\keyword{internal}
