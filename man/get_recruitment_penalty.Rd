% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_priors_penalties.R
\name{get_recruitment_penalty}
\alias{get_recruitment_penalty}
\title{Recruitment and initial age deviation penalties}
\usage{
get_recruitment_penalty(
  n_pop,
  n_regions,
  n_ages,
  n_est_rec_devs,
  rec_dd,
  natal_region,
  rec_region_prop_spec,
  rec_region_prop,
  equil_init_age_strc,
  ln_InitDevs,
  init_age_devs_shared,
  ln_sigmaR,
  bias_ramp,
  sigmaR_switch,
  ln_RecDevs,
  sigmaR2_early,
  sigmaR2_late,
  do_rec_bias_ramp,
  map_ln_RecDevs = NULL,
  RecDevs_pen_center = 0,
  InitDevs_pen_center = 0
)
}
\arguments{
\item{n_pop, n_regions, n_ages, n_est_rec_devs}{Dimension sizes.}

\item{rec_dd}{Integer recruitment density-dependence switch (used only to
pick \code{sigma_idx} when \code{n_pop == 1}).}

\item{natal_region}{Integer vector \code{[pop]} of natal region indices.}

\item{rec_region_prop_spec}{Integer switch; when \code{1}, populations/regions
with a fixed zero recruitment proportion are skipped.}

\item{rec_region_prop}{Array \code{[pop, region]} of recruitment regional
apportionment.}

\item{equil_init_age_strc}{Integer switch selecting which initial age
deviations are penalized (\code{1}: all but plus group, \code{2}: all,
\code{3}: shared subset).}

\item{ln_InitDevs}{Array \code{[pop, region, age]} of initial age deviations.}

\item{init_age_devs_shared}{Integer vector of shared initial age deviation
indices (used when \code{equil_init_age_strc == 3}).}

\item{ln_sigmaR}{Array \code{[early/late, pop, region]} of log-sigma for
recruitment deviations.}

\item{bias_ramp}{Numeric vector \code{[year]} of bias-ramp adjustment factors.}

\item{sigmaR_switch}{Integer year index at which recruitment deviations
switch from the early to the late sigma regime.}

\item{ln_RecDevs}{Array \code{[pop, region, year]} of recruitment deviations.}

\item{sigmaR2_early, sigmaR2_late}{Arrays \code{[pop, region]} of squared
sigma used for the bias-ramp mean offset.}

\item{do_rec_bias_ramp}{Integer switch enabling the bias-ramp log-sigma
adjustment.}

\item{map_ln_RecDevs}{Array \code{[pop, region, year]} mirroring
\code{map$ln_RecDevs}; cells that are \code{NA} are fixed rather than
estimated and are left unpenalized. \code{NULL} penalizes every cell.}
}
\value{
List with elements \code{Init_Rec_nLL} (array \code{[pop, region,
  age]}) and \code{Rec_nLL} (array \code{[pop, region, year]}), each holding
  negative log-likelihood penalties (0 where not penalized).
}
\description{
Population/region-specific IID penalties on initial age deviations
(\code{ln_InitDevs}) and on early/late recruitment deviations
(\code{ln_RecDevs}), including the Methot & Taylor bias-ramp adjustment.
Called once from the "Recruitment (Penalty)" section of \code{SPoRC_rtmb.R}.
}
\keyword{internal}
