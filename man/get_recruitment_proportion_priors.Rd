% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_priors_penalties.R
\name{get_recruitment_proportion_priors}
\alias{get_recruitment_proportion_priors}
\title{Dirichlet/beta priors on recruitment apportionment}
\usage{
get_recruitment_proportion_priors(
  use_rec_region_prop_prior,
  rec_region_prop_prior,
  rec_region_prop,
  use_rec_seas_prop_prior,
  use_fixed_rec_seas_prop,
  rec_seas_prop_prior,
  rec_seas_prop,
  rec_lag,
  spawn_seas,
  n_seas,
  use_stray_rate_prior,
  stray_rate_prior,
  stray_rate_pars
)
}
\arguments{
\item{use_rec_region_prop_prior}{Integer (0/1) switch for the regional
apportionment prior.}

\item{rec_region_prop_prior}{Data frame with columns \code{pop} and
\code{alpha} (list column of Dirichlet concentration vectors).}

\item{rec_region_prop}{Array \code{[pop, region]} of recruitment regional
apportionment.}

\item{use_rec_seas_prop_prior, use_fixed_rec_seas_prop}{Integer (0/1)
switches; the seasonal apportionment prior is skipped when seasonal
apportionment is fixed rather than estimated.}

\item{rec_seas_prop_prior}{Data frame with columns \code{pop} and
\code{alpha} (list column of Dirichlet concentration vectors).}

\item{rec_seas_prop}{Array \code{[pop, season]} of recruitment seasonal
apportionment.}

\item{rec_lag, spawn_seas, n_seas}{Integers controlling which seasons are
structurally zero (age-0 recruits before the spawning event) and so
excluded from the seasonal Dirichlet prior.}

\item{use_stray_rate_prior}{Integer (0/1) switch for the stray rate prior.}

\item{stray_rate_prior}{Data frame with columns \code{pop}, \code{block},
\code{mu} (prior mean, natural scale), \code{sd} (prior SD, natural scale).}

\item{stray_rate_pars}{Array \code{[pop, block]} of stray rate parameters on
the logit scale.}
}
\value{
Numeric scalar negative log-likelihood contribution, summed across
  all three prior sources.
}
\description{
Combines the recruitment regional apportionment prior, seasonal
apportionment prior, and stray rate prior, since all three feed the single
\code{rec_prop_nLL} accumulator in \code{SPoRC_rtmb.R}. Called once from the
"Recruitment Proportions (Prior)" / "Stray Rates (Prior)" sections.
}
\keyword{internal}
