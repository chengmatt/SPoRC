% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_growth.R
\name{growth_args_from_model}
\alias{growth_args_from_model}
\title{Assemble the growth module's arguments from the model's data and parameters}
\usage{
growth_args_from_model(env = parent.frame())
}
\arguments{
\item{env}{Environment holding the unpacked data and parameters, i.e. the
\code{SPoRC_rtmb} frame after \code{RTMB::getAll}. Defaults to the caller.}
}
\value{
A named list of arguments for \code{\link{Get_Growth}} and
  \code{\link{Get_Growth_Year}}.
}
\description{
The growth settings are the same for every call, whether the whole series is
built up front or one year at a time from inside the population loop, so they
are gathered once here rather than written out at each call site.
}
\keyword{internal}
