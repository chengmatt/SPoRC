% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diag_jitter.R
\name{jitter_start_values}
\alias{jitter_start_values}
\title{Draw jittered starting values for one jitter iteration}
\usage{
jitter_start_values(obj, par_vec, sd, jitter_random)
}
\arguments{
\item{obj}{An RTMB model object from \code{\link[RTMB]{MakeADFun}}.}

\item{par_vec}{Starting values, either the fixed-effect vector
(\code{length(obj$par)}) or the joint fixed and random vector
(\code{length(obj$env$par)}), or \code{NULL} for the model's own start.}

\item{sd}{Standard deviation of the additive normal draws.}

\item{jitter_random}{Whether the random effects are perturbed as well.}
}
\value{
A list with \code{fixed}, the jittered fixed-effect vector, and
  \code{random}, the random-effect starting values in \code{obj$env$random}
  order, jittered when \code{jitter_random = TRUE} and taken straight from
  \code{par_vec} otherwise. Length zero when the model has no random effects.
}
\description{
\code{nlminb()} only ever sees the fixed effects, so a jittered random effect
has to reach the model another way. TMB starts the inner Laplace solve from
\code{obj$env$last.par}, so the draws are returned separately and written
there by the caller.
}
\keyword{internal}
