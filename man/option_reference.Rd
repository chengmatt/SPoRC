% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_option_reference.R
\name{option_reference}
\alias{option_reference}
\title{Every argument the setup stages accept}
\usage{
option_reference(stages = setup_stage_order(), guide = NULL)
}
\arguments{
\item{stages}{Function names to document. Defaults to the eleven setup stages.}

\item{guide}{Path to the hand-written options guide, checked so the reference
can report which settings it also discusses. \code{NULL} skips the check.}
}
\value{
A data frame with one row per argument: \code{stage}, \code{argument},
  \code{default}, \code{description}, and \code{in_guide}.
}
\description{
Assembled from \code{formals()} and the package's own Rd, so it covers the API
as it currently stands rather than as it stood when someone last wrote it
down. Regenerating the vignette regenerates this.
}
\examples{
\dontrun{
ref <- option_reference()
subset(ref, stage == "Tagging")
}

}
