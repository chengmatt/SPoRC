% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_lik_at_age.R
\name{prep_at_age_obs}
\alias{prep_at_age_obs}
\title{Transform at-age observations onto the scale their likelihood is written on}
\usage{
prep_at_age_obs(obs, use, like_type, const = 0)
}
\arguments{
\item{obs}{Observation array, shaped like \code{use}.}

\item{use}{Integer array flagging which cells are fit.}

\item{like_type}{Integer per fleet. \code{0} lognormal, \code{1} normal.}

\item{const}{Small constant added inside the log of a lognormal cell.}
}
\value{
A numeric vector, one element per flagged cell in \code{which()}
  order, on the scale its fleet's likelihood uses.
}
\description{
A lognormal data source is fit on the log scale and a normal data source on the natural
scale, and the choice is per fleet, so the transformation is applied cell by
cell before \code{\link[RTMB]{OBS}} registration. Registration must happen
against the name \code{getAll} supplied, so the caller does it: a vector
registered under a local name does not link to the data element, and the
objective then diverges from the reported likelihood.
}
\keyword{internal}
