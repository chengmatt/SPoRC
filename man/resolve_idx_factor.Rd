% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_observations.R
\name{resolve_idx_factor}
\alias{resolve_idx_factor}
\title{Resolve the factor scale and loading for one simulated index cell}
\usage{
resolve_idx_factor(mvn, r, y, seas)
}
\arguments{
\item{mvn}{One fleet's element from \code{\link{build_idx_factor}}.}

\item{r, y, seas}{Integer indices of the simulated cell.}
}
\value{
List with scalars \code{d} and \code{lambda}.
}
\description{
Looks a cell up in a fleet's factor decomposition from
\code{\link{build_idx_factor}}. A cell inside the covariance gets its own
marginal scale and loading; a cell outside it (a projection year, or a cell
the fleet never fit) gets the mean scale and loading, so closed loop can
keep drawing the series past the end of the data.
}
\keyword{internal}
