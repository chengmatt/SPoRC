% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup_catchability.R
\name{setup_q_devs}
\alias{setup_q_devs}
\title{Set up catchability deviations for a fishery or survey}
\usage{
setup_q_devs(
  input_list,
  q_model,
  sigma_q_spec,
  q_rho_spec,
  q_rw_init_sigma,
  q_type,
  prefix,
  fleet_field,
  use_field,
  fleet_label,
  starting_values
)
}
\arguments{
\item{input_list}{List with \code{data}, \code{par} and \code{map}. The
catchability blocks, the index use arrays and \code{q_type} must already
be set.}

\item{q_model}{Character vector \code{[n_fleets]}, one of \code{"none"}
(default), \code{"iid"}, \code{"rw"}, \code{"ar1"} or \code{"dsem"}.}

\item{sigma_q_spec}{Sharing string for the deviation sigma over region and
fleet. Default \code{"est_all"}.}

\item{q_rho_spec}{Sharing string for the ar1 correlation. Default
\code{"est_all"}.}

\item{q_rw_init_sigma}{Standard deviation of the first estimated year of a
random walk. \code{NA} (default) starts the walk at zero under its own
sigma, which keeps the block catchability as the level of the series.}

\item{q_type}{Character vector \code{[n_fleets]} of \code{"est"},
\code{"arith"} or \code{"geo"}, as already validated by the caller.}

\item{prefix}{\code{"fish"} or \code{"srv"}.}

\item{fleet_field}{Name of the fleet count in \code{data}.}

\item{use_field}{Name of the index use array in \code{data}.}

\item{fleet_label}{Label used in messages.}

\item{starting_values}{Named list of starting values.}
}
\value{
\code{input_list} with the deviation parameters, their maps and
  \code{<prefix>_q_model} in \code{data}.
}
\description{
Builds the deviation array, its sigma and correlation, and refuses the
combinations that cannot be identified. Called by
\code{\link{Setup_Mod_Fishsel_and_Q}} and
\code{\link{Setup_Mod_Srvsel_and_Q}}.
}
\keyword{internal}
