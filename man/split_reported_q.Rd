% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_catchability.R
\name{split_reported_q}
\alias{split_reported_q}
\title{Split a fit's reported catchability into its mean and its deviations}
\usage{
split_reported_q(rep_q, ln_q, q_blocks, q_type = NULL)
}
\arguments{
\item{rep_q}{Reported catchability \code{[region, year, fleet]}.}

\item{ln_q}{Log block catchability \code{[region, block, fleet]}.}

\item{q_blocks}{Block index \code{[region, year, fleet]}.}

\item{q_type}{Integer vector \code{[fleet]}, zero where catchability is
estimated and non-zero where it is solved from the index.}
}
\value{
List of \code{q_mean} and \code{devs}, both
  \code{[region, year, fleet]}.
}
\description{
The reported catchability is \eqn{\exp(\ln q_{blk} + \epsilon)}. The mean is
the level a process error walks from, the deviations are what the
conditioning years reproduce, and an analytically solved fleet reads no
\code{ln_q} so its reported value is already its mean.
}
\keyword{internal}
