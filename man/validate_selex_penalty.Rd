% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_setup.R
\name{validate_selex_penalty}
\alias{validate_selex_penalty}
\title{Validate a selectivity fixed-effect centering penalty table}
\usage{
validate_selex_penalty(selex_penalty, use_flag, what)
}
\arguments{
\item{selex_penalty}{Data frame with columns \code{region}, \code{fleet},
\code{block}, \code{sex}, \code{par}, and \code{wt}, or \code{NULL}.}

\item{use_flag}{Integer (0/1). When \code{0} the table is returned unchanged
and never validated, matching how the prior tables are guarded.}

\item{what}{Character. Name used in error messages.}
}
\value{
The validated table with \code{par} as a list column, or the input
  unchanged when \code{use_flag} is \code{0}.
}
\description{
Checks the table consumed by \code{\link{get_selex_fixed_penalty}} and
normalizes its \code{par} column to a list of integer vectors, so a row may
name either one parameter or a whole set.
}
\keyword{internal}
