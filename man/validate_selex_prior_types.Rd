% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_setup.R
\name{validate_selex_prior_types}
\alias{validate_selex_prior_types}
\title{Validate the type column of a selectivity prior table}
\usage{
validate_selex_prior_types(selex_prior, use_flag, what, sel_blocks, n_bins)
}
\arguments{
\item{selex_prior}{Data frame with columns \code{region}, \code{fleet},
\code{block}, \code{sex}, \code{par}, \code{mu}, \code{sd}, and optionally
\code{type}, or \code{NULL}.}

\item{use_flag}{Integer (0/1). When \code{0} the table is returned unchanged
and never validated, matching how the prior tables are guarded.}

\item{what}{Character. Name used in error messages.}

\item{sel_blocks}{Integer array \code{[region, year, fleet]} mapping model
years to selectivity blocks.}

\item{n_bins}{Integer. Size of the grid the data source's selectivity is
parameterized on (ages or lengths per its selectivity type).}
}
\value{
The validated table, or the input unchanged when \code{use_flag} is
  \code{0}.
}
\description{
Checks the optional \code{type} column consumed by
\code{\link{get_selex_prior}}. A table without the column is all
\code{"par"} rows (the original behavior) and passes untouched.
\code{"value"} rows are range-checked here because their \code{par} column
indexes the selectivity grid rather than the parameter vector, and their
\code{block} must exist in the fleet's block map to resolve to a year.
}
\keyword{internal}
