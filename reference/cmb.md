# For combining a parameter and data list in RTMB so a data object can be explicitly defined

For combining a parameter and data list in RTMB so a data object can be
explicitly defined

## Usage

``` r
cmb(f, d)
```

## Arguments

- f:

  Parameter list

- d:

  Data list
