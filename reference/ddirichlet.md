# Dirichlet Likelihood

Dirichlet Likelihood

## Usage

``` r
ddirichlet(x, alpha, log = TRUE)
```

## Arguments

- x:

  vector of values

- alpha:

  Expected values w/ concentration sum(alpha)

- log:

  Whether to give log or not

## Value

Returns likelihood values from a Dirichlet distribution
