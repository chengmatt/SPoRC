# Helper function to set up fishery length overdispersion correlation parameters

Helper function to set up fishery length overdispersion correlation
parameters

## Usage

``` r
do_FishLen_corr_pars_mapping(input_list)
```

## Arguments

- input_list:

  Input list
