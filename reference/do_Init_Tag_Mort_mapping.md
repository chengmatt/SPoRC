# Helper function to setup initial tag mortality parameters

Helper function to setup initial tag mortality parameters

## Usage

``` r
do_Init_Tag_Mort_mapping(input_list, Init_Tag_Mort_spec)
```

## Arguments

- input_list:

  Input list

- Init_Tag_Mort_spec:

  Character vector specifying initial tag mortality parameterization
