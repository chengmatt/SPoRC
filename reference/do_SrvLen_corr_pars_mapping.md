# Helper function to set up survey length overdispersion correlation parameters

Helper function to set up survey length overdispersion correlation
parameters

## Usage

``` r
do_SrvLen_corr_pars_mapping(input_list)
```

## Arguments

- input_list:

  Input list
