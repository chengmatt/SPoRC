# Helper function to set up tag reporting rate parameters

Helper function to set up tag reporting rate parameters

## Usage

``` r
do_Tag_Reporting_Pars_mapping(input_list, TagRep_spec)
```

## Arguments

- input_list:

  Input list

- TagRep_spec:

  Charcacter specifying tag reporting parameterization
