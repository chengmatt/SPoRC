# Helper function to set up tag shedding parameters

Helper function to set up tag shedding parameters

## Usage

``` r
do_Tag_Shed_mapping(input_list, Tag_Shed_spec)
```

## Arguments

- input_list:

  Input list

- Tag_Shed_spec:

  Character specifying tag shedding parameterization
