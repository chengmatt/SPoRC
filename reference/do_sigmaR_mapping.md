# Title Helper function to handle sigmaR mapping

Title Helper function to handle sigmaR mapping

## Usage

``` r
do_sigmaR_mapping(input_list, sigmaR_spec)
```

## Arguments

- input_list:

  Input list

- sigmaR_spec:

  Character vector for specifying sigmaR mapping

## Value

Input list with mapping modified
