# Helper function to set up tag overdispersion parameters

Helper function to set up tag overdispersion parameters

## Usage

``` r
do_tag_theta_mapping(input_list)
```

## Arguments

- input_list:

  Input list
