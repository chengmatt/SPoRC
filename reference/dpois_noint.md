# Poisson Likelihood

Approximates Poisson when non-integer values are provided

## Usage

``` r
dpois_noint(x, pred, give_log = TRUE)
```

## Arguments

- x:

  observations

- pred:

  predicted

- give_log:

  if giving log likelihood

## Value

Returns likelihood values from a poisson
